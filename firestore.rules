rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /submissions/{document=**} {
      // TEMP: allow full access for development / trusted admin-only usage
      allow read, write: if true;
    }
  }
}
